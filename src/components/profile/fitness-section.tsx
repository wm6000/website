export function FitnessSection() {
  return (
    <section aria-labelledby="fitness-heading" className="flex flex-col gap-2">
      <h2 id="fitness-heading" className="text-lg font-semibold">
        Fitness
      </h2>
      <p className="text-sm text-muted-foreground">
        Long-term goals, target events, and physical limitations — coming
        soon.
      </p>
    </section>
  );
}
